### Git的使用

```js
<!-- 初始化 -->
git init  // # 上传所有文件 

git add .   // # 提交备注

git commit -m 'xxx'  // # 上传到github

git pull origin master --allow-unrelated-histories   //强制提交

git push origin master  // # 提交
```